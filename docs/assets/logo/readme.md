# gotpm logo — 6b (dark, lifted crate)

Gopher standing behind a strapped freight crate. Slate gopher, lifted-edge dark crate, one red strap.

| File | Use |
| --- | --- |
| gotpm-banner.svg | 470×150 horizontal lockup, transparent background |
| gotpm-banner-dark.svg | Same lockup on the ink ground (#1c1d1f) |
| gotpm-mark.svg | Mark alone, 240×240, transparent — for custom crops |
| gotpm-favicon.svg | Round favicon / avatar, good down to ~32px |
| gotpm-favicon-small.svg | Simplified mark for 16–24px |

## Colors

- ground `#1c1d1f`
- gopher `#b6bcc0` body, `#dfe3e5` belly, `#8a8f93` ears
- crate `#2f3135` face, `#3b3e43` rails/posts, `#787d84` edges
- strap / accent `#e2564a`
- wordmark `#efe9df`, tagline `#8d9298`

## Notes

Wordmark is Inter 500 at -2 tracking; the "t" of gotpm takes the strap red. Below ~24px use gotpm-favicon-small.svg — it drops the crate slats, label and belly. Clear space: half the crate height on all sides.
